# EVOLUTION

本次设计选择是：

1. Sudoku 负责规则、候选推理、冲突与矛盾检测、局面指纹。
2. Game 负责会话状态切换、历史管理、探索子会话生命周期。
3. UI 通过 game store 调用领域接口，不在组件中临时拼规则。

---

## 1. 提示功能如何实现

### 1.1 候选提示

- 候选提示由 Sudoku 提供：

1. getCandidates(row, col)
2. getAllCandidates()

- 实现方式：

1. 对目标空格收集同一行、同一列、同一宫已使用数字。
2. 从 1 到 9 过滤掉已使用值，剩余即候选集合。

- 好处：

1. 候选计算与 UI 脱钩。
2. 同一份规则可复用于主局面与探索局面。

### 1.2 下一步提示

- 下一步提示同样由 Sudoku 提供：

1. getDeterministicMoves()
2. getNextDeterministicMove()

- 实现方式：

1. 遍历所有空格候选。
2. 候选数量为 1 的格子就是可直接确定步。

### 1.3 仅提示位置 与 直接填写答案 的区分

- 由 Game 暴露两个不同语义接口：

1. getNextHintPosition 返回 row、col、candidates、type=position
2. getNextHintAnswer 返回 row、col、value、type=answer

- 提供 applyHint 或 applyHintAnswer 来实际落子。

- UI 层对应两个按钮：

1. 候选数提示按钮：把候选写入候选显示区。
2. 直接答案提示按钮：执行落子。

### 1.4 提示能力归属

Sudoku 与 Game 协作：

1. Sudoku 负责**规则与推理能力**（候选、唯一步、矛盾）。
2. Game 负责**以会话语义对外暴露**（当前在哪个会话、是否探索中、是否可应用）。

### 1.5 实现过程中遇到的问题

1. UI 最初仍走旧提示路径，和新领域接口语义不一致，导致提示表现不稳定。
2. 直接答案提示在选中格不可填时容易给出无反馈，后续增加了可用回退逻辑。
3. clone 与 JSON 如果不保留 givens 掩码，反序列化后会误判固定格，影响提示与落子。

解决方式：

1. 将提示入口统一收敛到 game store 对领域接口的转发。
2. 修复 Sudoku 的 givens 在 clone 和 JSON 往返中的完整保留。
3. 保持 UI 只调用 store，不在组件内重写提示规则。

---

## 2. 探索模式如何实现

### 2.1 探索模式的本质

探索模式本质是：

1. Game 进入新的状态（main 或 explore）。
2. Game 持有一个**临时子会话** exploreSession。
3. 子会话**基于快照启动，支持独立历史和回滚**。

这同时属于 ==状态切换 + 分支会话== 的组合实现。

### 2.2 主局面与探索局面的关系

1. 不是共享对象，而是**复制对象**。
2. 进入探索时创建：
   - base：探索起点快照
   - present：探索当前盘面
   - past/future：探索内历史

不共享原因：

1. 避免引用污染。
2. 保证放弃探索时主局面不被污染。

提交与放弃：

1. commitExplore：把探索 present 合并回主局面，并写入主 history。
2. discardExplore：直接丢弃探索会话，回到主局面。
3. rollbackExplore：回到探索 base，不影响主局面。

### 2.3 冲突判断

探索失败判断分两类：

1. 显式冲突：行列宫重复值。
2. 隐式矛盾：空格无候选。

由 Sudoku.hasContradiction 提供统一判断。

### 2.4 回溯能力

回溯有两层：

1. 探索内 undo/redo：针对探索内局部操作。
2. rollbackExplore：一键回到探索起点。

用户可快速重试其他候选路径。

### 2.5 记忆失败路径

失败路径记忆通过 局面指纹 完成：

1. Sudoku.getStateFingerprint 生成当前局面标识。
2. Game 在探索失败时将指纹写入 failedExploreFingerprints。
3. 后续再次走到同一指纹，状态返回 knownFailed=true。
4. UI 显示 已命中历史失败路径 提示。

---

## 3. 对象模型一致性如何保持

保留核心结构：

1. Sudoku 仍是核心领域对象。
2. Game 仍是会话与历史管理对象。
3. UI 仍通过 store 调用领域对象。

演进方式是扩展，不是重写：

1. 在 Sudoku 增加提示与矛盾检测能力。
2. 在 Game 增加探索状态与子会话。
3. 不把关键逻辑塞回组件。

---

## 4. history 如何演进

### 4.1 结构变化

1. 主 history 仍是线性 past/future。
2. 探索会话有独立 past/future。
3. 未引入树状分支持久结构。

### 4.2 语义变化

1. 探索过程的撤销重做只作用于探索会话。
2. commitExplore 时把探索最终结果作为一次主会话提交。
3. discardExplore 不会污染主会话 history。

因此本次是 ==线性 history + 子会话局部线性 history== 的组合。

---

## 5. 问题与回答

### 5.1 你如何实现提示功能

通过 Sudoku 计算候选与唯一步，通过 Game 暴露位置提示与答案提示，再由 store 与 UI 按按钮区分调用。

### 5.2 你认为提示功能更属于 Sudoku 还是 Game，为什么

规则与推理属于 Sudoku；会话语义与对外入口属于 Game，因此是**协作关系**。

### 5.3 你如何实现探索模式

Game **增加explore 状态与临时子会话，使用快照、独立 history、提交/放弃/回滚机制，结合矛盾检测与失败路径记忆**。

### 5.4 主局面与探索局面的关系是什么

**复制关系**，不共享引用；提交时合并，放弃时丢弃，回滚时回到探索起点快照。

### 5.5 history 结构是否发生变化

发生变化：从单线性 history 扩展为 **主线性 history + 探索子会话线性 history**。

### 5.6 Homework 1 哪些设计暴露局限

缺少提示推理能力、缺少探索分支语义、缺少局面级矛盾与指纹信息。

### 5.7 如果重做 Homework 1，会如何修改

优先做可扩展会话模型与领域接口分层，把候选/矛盾/指纹作为基础能力前置。

---
（课堂）

### 5.8 为什么提示和探索会推动对象设计变化

因为它们引入了**规则推理与分支会话**两种新语义，超出了单次落子与线性撤销模型，必须重构职责边界。

### 5.9 探索模式更接近状态切换还是分支会话

**状态切换驱动下的分支会话**：状态决定路由到哪个会话，会话承载局面与历史。

### 5.10 设计最脆弱的地方在哪里

store 与 UI 调度边界最脆弱；如果 UI 绕过 store 直接改数据，会破坏领域语义一致性。

### 5.11 history 结构是否还能扩展

可以扩展到树状分支，但是保持线性更稳妥、可维护。

### 5.12 迁移到新的响应式机制时，哪些易迁移，哪些最难

最易迁移：Sudoku 与 Game 领域层，因为不依赖具体框架。
最难迁移：store 到组件的响应式调度层，因为绑定了当前框架的数据流与订阅机制。

---

## 6. 与评分标准的对齐说明

### 6.1 提示功能正确性

1. 候选提示与下一步提示可用。
2. 区分位置提示和答案提示。
3. 行为稳定并有领域接口支撑。

### 6.2 探索模式正确性

1. 可进入探索。
2. 可提交与放弃。
3. 可回溯到探索起点。
4. 支持冲突判定与失败路径记忆。

### 6.3 状态与对象协作设计

1. Sudoku 负责规则推理。
2. Game 负责会话状态与历史。
3. UI 不承载关键领域逻辑。

### 6.4 history 演进合理性

1. 主会话与探索会话历史分离。
2. 提交与放弃语义清晰。
3. 通过 clone 和序列化避免引用污染。

